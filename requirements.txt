# ═══════════════════════════════════════════════════════════════════
#  Platinum Sales Chatbot — Requirements
#  Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════

# ── Core ML (scratch LLM — train.py / scratch_llm.py) ─────────────
torch>=2.1.0                  # neural network, training loop, inference
scikit-learn>=1.3.0           # TfidfVectorizer + cosine_similarity (retriever)
numpy>=1.24.0                 # array ops used by retriever

# ── Data handling ──────────────────────────────────────────────────
pandas>=2.0.0                 # CSV loading, dataframe operations

# ── Web API (api.py) ───────────────────────────────────────────────
django>=4.2.0                 # HTTP server, routing, views
djangorestframework>=3.14.0   # REST framework used in INSTALLED_APPS
django-cors-headers>=4.3.0    # CORS middleware (CORS_ALLOW_ALL_ORIGINS)

# ── Document parsing (document_parser.py / attachment_handler.py) ─
pypdf>=3.0.0                  # PDF text extraction
python-docx>=1.1.0            # .docx Word document parsing
openpyxl>=3.1.0               # .xlsx Excel file reading (used by pandas)
xlrd>=2.0.1                   # .xls legacy Excel reading (used by pandas)

# ── Image handling (vision_parser.py / attachment_handler.py) ─────
Pillow>=10.0.0                # image open / convert (PIL.Image)
pytesseract>=0.3.10           # OCR wrapper — also needs Tesseract binary:
                              #   Windows : https://github.com/UB-Mannheim/tesseract/wiki
                              #   Linux   : sudo apt install tesseract-ocr
                              #   macOS   : brew install tesseract

# ── Utilities ──────────────────────────────────────────────────────
pathlib2>=2.3.7; python_version < "3.10"   # backport (not needed on 3.10+)
