# ── Web framework ─────────────────────────────────────────────────────────
Django>=4.2,<5.2
django-cors-headers>=4.3
# NOTE: djangorestframework was in the old INSTALLED_APPS but nothing in
# this codebase uses DRF (every view is a plain django.views.View +
# JsonResponse). Removed from api.py and from here — if that changes,
# add it back.

# ── Data / ML (all local — no external API calls, no pretrained weights) ──
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3          # TF-IDF retrieval (SmartFallbackEngine, rag_utils)
torch>=2.1                 # scratch-trained SalesGPT — CPU build by default;
                            # for CUDA, install per https://pytorch.org/get-started/locally/
                            # instead of the plain PyPI wheel.

# ── Document / attachment parsing ──────────────────────────────────────────
pypdf>=4.0                 # PDF text extraction
python-docx>=1.1           # imported as `docx` — .docx extraction
openpyxl>=3.1              # required by pandas for .xlsx (not imported directly)
xlrd>=2.0                  # required by pandas for legacy .xls (not imported directly)
Pillow>=10.0                # imported as `PIL` — image handling

# ── OCR ──────────────────────────────────────────────────────────────────
pytesseract>=0.3.10
# pytesseract is a wrapper around the Tesseract OCR binary, which is NOT a
# pip package. Install it separately:
#   Ubuntu/Debian : sudo apt-get install tesseract-ocr
#   macOS         : brew install tesseract
#   Windows       : https://github.com/UB-Mannheim/tesseract/wiki
# Without it, vision_parser.py silently falls back to pixel-heuristic
# image descriptions (no OCR, no text-based Q&A on images).
